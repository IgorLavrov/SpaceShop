@model SpaceShop.Models.RealEstate.ImageToDatabaseViewModel

@*@{
  //  var photopath = $"data:image/gif;base 64,{Convert.ToBase64String(Model.ImageData)}";

    //}*@

<div class="form-group row col-sm-4 offset-2">
    <div>
        @Model.ImageTitle
    </div>
    <img class="card-img-top"src="@Model.Image" asp-append-version="true"/>
</div>
